apiVersion: v1
kind: Pod
metadata:
  name: seannguyen.helloworld
  labels:
    app: helloworld
spec:
  containers:
  - name: hello-world
    image: seannguyen/hello-world
    ports:
    - name: web-port
      containerPort: 5000
